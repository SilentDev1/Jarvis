# Devices

The generic device registry stores ID, type, module, provider, status, and last-seen time. Initial devices are the Tapo C101 camera and an AiPi placeholder. Modules use provider interfaces rather than device classes directly. Device health is exposed at `/health/devices`.

