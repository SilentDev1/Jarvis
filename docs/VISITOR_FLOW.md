# Visitor flow

`EMPTY → PERSON_DETECTED → APPROACHING → DWELLING → GREETING → CONVERSATION → WAITING/WAITING_FOR_BADGE → SESSION_COMPLETE`.

Brief passersby never reach dwell. One session ID survives short occlusion. Conversation structure is authoritative; the model receives it but cannot redefine policy. Departure finalizes the record and notification summary.

