# Privacy

Defaults: event snapshots on, transcripts on, badge images only when requested, raw audio off, face recognition off, 30-day media retention. Continuous analysis does not imply continuous recording. Deletion consists of removing a session's referenced media and database records; automate this only after production paths and backups are verified.

