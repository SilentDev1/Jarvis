# Server deployment

Use Docker Compose on Linux. Supply a new `.env`, mount persistent data/log volumes, point device and AI URLs at resolvable LAN services, run migrations/tests, then health checks. Accelerator-specific vision may run natively/sidecar behind the same interface.

