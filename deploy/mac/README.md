# macOS deployment

Use the root scripts and native Python environment. Keep Ollama native for Metal acceleration. Do not enable launchd until manual camera/simulator operation is verified. A future launch agent should invoke `scripts/start.sh` with its working directory set to the repository.

